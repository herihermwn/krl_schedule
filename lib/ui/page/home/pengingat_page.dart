part of '../pages.dart';

class PengingatView extends StatelessWidget {
  const PengingatView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}

